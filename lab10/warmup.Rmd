```{r}
library(tidyverse)
library(naniar)
library(janitor)
```


```{r}
malaria <- read_csv("data/malaria.csv") %>% (clean_names)
```
```{r}
#malaria_long <- malaria %>% 
  #pivot_longer(cols=starts_with("malaria_rdt"), 
                               # names_to = "age_class",
                                #3values_to = "cases") %>%
              # select(newid, data_date, subitted_date, location_name, province district, age-class, cases)
```
```{r}
#malaria_long %>%
  #filter(data_date=="2020-07-30") %>%
  #group_by(district) %>%
  #summarise(tot_cases=sum(cases, na.rm = T)) %>%
  #arrange(-tot_cases)
```

